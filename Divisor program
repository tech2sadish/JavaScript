num = prompt (" Enter any number ");
divisor = 2; // Edit if you want to start with 1
count = 0;
while(divisor<=num)
{
	if(num%divisor==0)
	{
		console.log(divisor)
		count++
		
	}
divisor++;
}
console.log(count)
